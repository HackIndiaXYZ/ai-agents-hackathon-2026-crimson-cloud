# ── RAG engine deps (already installed) ─────────────────────────────────────
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
langchain-huggingface>=0.1.0
langchain-qdrant>=0.2.0
sentence-transformers>=3.0.0
torch                             # CPU build: install via --index-url https://download.pytorch.org/whl/cpu
qdrant-client>=1.9.0
pandas>=2.0.0

# ── LLM chain deps (new) ────────────────────────────────────────────────────
google-generativeai>=0.7.0        # Gemini SDK  (pip name: google-generativeai)
pydantic>=2.0.0                   # schema validation (already pulled by langchain)
pillow>=10.0.0                    # image/OCR support (optional – only needed for image_bytes path)

# ── FastAPI (for downstream API server) ─────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9           # for file uploads in FastAPI
